// Frontend feature flags. Flip these locally; changes require a rebuild.
// Keep the list short — flags that outlive their purpose should be deleted, not left as `true`.

export const ENABLE_EVAL_TAB = false;
export const ENABLE_DASHBOARD_LINK = false;
